package heap;

/**
 * This file defines a class named with {@linkplain HeapManager} which served as
 * dynamically allocating memory from HeapManager area with response to request
 * for creating new object, like Array(current, not supported), variable used in
 * internal and others.
 *
 * @author Xlous.zeng
 */
public abstract class HeapManager
{
	
}
