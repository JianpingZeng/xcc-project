foreach(t ${XCC_TARGETS_TO_BUILD})
  message(STATUS "Targeting ${t}")
  string(TOLOWER ${t} LOWER_TARGET)
  add_subdirectory(${LOWER_TARGET})

  set(TARGET_SRCS ${TARGET_SRCS} ${LOCAL_TARGET_SRCS} PARENT_SCOPE)
  set(TABLEGEN_OUTPUT ${TABLEGEN_OUTPUT} ${LOCAL_TABLEGEN_OUTPUT} PARENT_SCOPE)
  set(BACKEND_TARGET ${BACKEND_TARGET} ${LOCAL_BACKEND_TARGET} PARENT_SCOPE)
endforeach()